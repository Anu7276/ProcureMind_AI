# CI-pinned requirements — minimal subset needed for tests (no GPU/heavy deps)
# Generated by Phase 6 CI/CD hardening

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# LangGraph + LangChain (mock mode — no actual LLM calls in CI)
langgraph==0.2.28
langchain-core==0.3.10

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# Document extraction (lightweight)
python-docx==1.1.2

# Data
tqdm==4.66.5

# DB drivers — skipped in CI (services are mocked/degraded)
# asyncpg, sqlalchemy, neo4j, qdrant-client: NOT installed in CI
