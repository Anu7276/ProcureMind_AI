# ═══════════════════════════════════════════════════════════
# BIS Standards Recommendation Engine — Python Dependencies
# ═══════════════════════════════════════════════════════════

# ── Core API ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
aiofiles>=23.2.0
httpx>=0.27.0

# ── LangGraph + LangChain ─────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-google-genai>=1.0.0      # Google Gemini (primary)
langchain-openai>=0.1.0            # OpenAI (alternative)
langchain-anthropic>=0.1.0         # Anthropic (alternative)
langchain-groq>=0.1.4              # Groq (alternative)
langchain-community>=0.2.0

# ── Database — PostgreSQL ─────────────────────────────────
sqlalchemy>=2.0.0
asyncpg>=0.29.0                    # async PostgreSQL driver
psycopg2-binary>=2.9.9             # sync driver (ingestion script)

# ── Database — Vector Store ───────────────────────────────
qdrant-client>=1.12,<2

# ── Database — Knowledge Graph ────────────────────────────
neo4j>=5.20.0

# ── Embeddings (local, no API cost) ──────────────────────
sentence-transformers>=3.0.0
# torch is a dependency of sentence-transformers; CPU-only is fine for dev:
# pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── Document Understanding ────────────────────────────────
pymupdf>=1.24.0                     # PDF parsing and table extraction (primary)
pypdf>=4.0.0                        # PDF parsing fallback
python-docx>=1.1.0                  # DOCX parsing and table extraction
pytesseract>=0.3.10                # OCR for scanned images and PDFs
Pillow>=10.3.0                     # image handling
# Note: Docling is optional and moved to requirements-docling.txt
# Note: Tesseract binary must be installed separately for OCR:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   Ubuntu:  sudo apt install tesseract-ocr

# ── Config + Utils ────────────────────────────────────────
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
langdetect>=1.0.9                  # language detection in Node01

# ── Evaluation ────────────────────────────────────────────
tqdm>=4.66.0
tabulate>=0.9.0

# ── Dev / Testing ─────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
